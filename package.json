{
  "name": "stripe-subscription-canceller",
  "version": "1.0.0",
  "description": "Servidor Express que escucha webhooks de Stripe y programa la cancelación automática de suscripciones según el plan de cuotas contratado.",
  "main": "webhook-server.js",
  "scripts": {
    "start": "node webhook-server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^16.0.0"
  }
}
